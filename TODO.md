* Delete some struct:
  * `VArchiveErr` (Replaced to `APIError`)
* Changes terms (Ref: [https://iidx.org/terminology])
  * Pattern -> Chart
  * `title` -> `song_id`
  * (on related) `name` -> `title`
