# v_arch_rs

This is Rust library for using [V-Archive](https://v-archive.net/). It uses [their API](https://v-archive.net/info/api)

This here is just only for a user's tier information. Other function will be support later.
